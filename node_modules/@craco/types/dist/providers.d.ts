import type { Configuration as DevServerConfig } from 'webpack-dev-server';
import type { Config as JestConfig } from '@jest/types';
export declare type DevServerConfigProvider = (proxy: any, allowedHost: string) => DevServerConfig;
export declare type JestConfigProvider = (customResolve: (relativePath: string) => string, projectRoot: string, isEjecting: boolean) => JestConfig.InitialOptions;
